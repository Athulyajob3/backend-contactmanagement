 {
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "license": "ISC",
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  },
  "scripts": {
    "start": "npx json-server --watch db.json --port $PORT"
  }
}
